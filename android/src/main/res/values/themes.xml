<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      LOCK46 is dark-only by design; see the comment on Lock46Theme in Theme.kt.
      The parent is a platform theme rather than an AppCompat one because the only
      Activity extends ComponentActivity and the UI is entirely Jetpack Compose.
    -->
    <style name="Theme.LOCK46" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/lock46_ink</item>
        <item name="android:colorBackground">@color/lock46_ink</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
