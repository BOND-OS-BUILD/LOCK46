<?xml version="1.0" encoding="utf-8"?>
<!--
  Foreground-app detection only.

  canRetrieveWindowContent is false and no flags that expose screen content are set:
  LOCK46 reads AccessibilityEvent.packageName and nothing else. Only window-state
  changes are subscribed to, which is the smallest event set that still tells us
  when a different app comes to the front.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="0"
    android:summary="@string/accessibility_service_description" />
